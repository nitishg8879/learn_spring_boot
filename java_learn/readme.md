git remote add origin git@personal_github:nitishg8879/learn_spring_boot.git


to create object we need class
so class is a blueprint or template of an object to be created. 



extend ko inherit khte hai
implment ko abstraction


inheritance mai kuch type hota like
a>b isko single 
a>b>c multilevel
    a
c        b   ---> heiraacel 

polymorphism means 
poly means "many" mophisim means form
matlab kahi chize ke ek hi matlab hai

there are 2 types compile/runtime polymorphisim.
compile means overloading
runtime means overriding


is-a realtionship mean inheritance
class B is-a child of class A

has-a relationship
one-to-one
one-to-many
many-to-many

assocation relation between
if object can surview indpedently then it's called strong aggregation
if object depent on another and we delete parent then it automatically delelte child it's called the weak composition



deque

List
arraylist
linkedlist
double linked list


hashmap
linkedhashmap

deamon thread
something which is running async using parallel
user thread and deamon thread
whatever we create normally that's called user thread
if want to make the thread deaom then we have to set 
main thread is consider as user thread


