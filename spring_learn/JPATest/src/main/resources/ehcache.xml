<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xmlns:jsr107="http://www.ehcache.org/v3/jsr107"
        xsi:schemaLocation="
            http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">

    <!-- Default cache template applied to regions not explicitly configured -->
    <cache-template name="default">
        <expiry>
            <ttl unit="seconds">300</ttl>
        </expiry>
        <resources>
            <heap unit="entries">1000</heap>
            <offheap unit="MB">10</offheap>
        </resources>
    </cache-template>

    <!-- 2nd-level cache region for the User entity -->
    <cache alias="com.JPA.JPATest.entity.User" uses-template="default">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">500</heap>
            <offheap unit="MB">5</offheap>
        </resources>
    </cache>

    <!-- Query cache region -->
    <cache alias="org.hibernate.cache.internal.StandardQueryCache" uses-template="default"/>

    <!-- Timestamps region required when query cache is enabled -->
    <cache alias="org.hibernate.cache.spi.UpdateTimestampsCache" uses-template="default">
        <expiry>
            <none/>
        </expiry>
    </cache>

</config>
